const Main = ({ children }) => {
  // This component is used to wrap the main content of the page, providing a consistent layout and styling.
  return <div className="main-content">{children}</div>;
};

export default Main;